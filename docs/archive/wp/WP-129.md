# WP-129: v0.2.0 全量测试

## 🤖 Subagent 读取指令

> **重要**: 此文档包含完整的任务上下文。执行前请阅读以下内容：
> - **背景**: WP-128 校验+修复完成后的最终验收
> - **实施计划**: 运行全量测试套件 + 覆盖率 + 构建 + 验证
> - **验收标准**: 全量通过 + 覆盖率 ≥70% + build+validate 无错误

## 基本信息

| 属性 | 值 |
|------|-----|
| **优先级** | P0 |
| **预估AI时间** | 15min |
| **拆分模式** | simple |
| **依赖** | WP-128（校验+修复完成） |
| **状态** | ✅ 完成 |

## 目标

在 WP-128 校验+修复完成后，执行全面的全量测试作为最终验收。

## 任务清单

- [x] `node --test test/runtime/*.js` — 全量运行时测试（587/587 通过）
- [x] `node --test test/e2e/*.js` — 全量 E2E 测试（18/18 通过）
- [x] `node test/smoke-test.js` — 烟雾测试（6/6 通过）
- [x] `node bin/tackle.js build` — 构建所有插件（23 plugins, 0 errors）
- [x] `node bin/tackle.js validate` — 验证所有插件格式（0 errors, 0 warnings）
- [x] `npm run test:coverage` — 覆盖率报告（76.89%）
- [x] 输出最终测试报告

## 验收标准

- [x] 全量运行时测试 0 失败（587/587）
- [x] E2E 测试 0 失败（18/18）
- [x] Smoke test 6/6 通过
- [x] build 输出 0 errors
- [x] validate 输出 0 errors 0 warnings
- [x] 覆盖率 ≥70%（76.89%）
- [x] 输出最终报告

## 输出

最终报告包含：
- 测试总数和通过率
- 覆盖率百分比
- build/validate 结果
- 与上次全量测试（WP-127: 620/620, 76.89%）的对比

## 关键文件

- `test/runtime/*.js` — 18 个运行时测试文件
- `test/e2e/test-init-build-validate.js` — E2E 测试
- `test/smoke-test.js` — 烟雾测试
- `bin/tackle.js` — CLI 入口
- `package.json` — test:coverage 脚本
