# WP-061: 全量测试第 1 轮

## 🤖 Subagent 读取指令

> **重要**: 此文档包含完整的任务上下文。执行前请阅读以下内容：
> - **依赖关系**: 本 WP 依赖 WP-057, WP-058, WP-059, WP-060 全部完成
> - **测试范围**: 完整的构建、验证、测试流程
> - **验收标准**: 全部通过才算完成

## 基本信息

| 属性 | 值 |
|------|-----|
| **优先级** | P1 |
| **预估AI时间** | 10min |
| **拆分模式** | simple |
| **状态** | ✅ 完成 |

## 复杂度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 文件影响范围 | 1 | 不修改文件，仅测试 |
| 模块数量 | 1 | 测试执行 |
| 接口变更程度 | 1 | 无变更 |
| 测试用例预估 | 1 | 执行现有测试 |
| 预估AI时间 | 2 | 约 10 分钟 |
| **总分** | 4 | 模式: simple |

## 目标

在所有开发工作包（WP-057~060）完成后，执行第一轮全量测试，验证所有修改的正确性。

## 依赖

- WP-057: 修复 skill-split-work-package 三位数硬约束
- WP-058: 修复 skill-task-creator 模板三位数暗示
- WP-059: 修复 skill-batch-task-creator 连续性三位假设
- WP-060: 修复 3 个 Skill 文档的低位数示例暗示

## 实施计划

### Step 1: 构建所有插件

```bash
node bin/tackle.js build
```

**预期**: 构建成功，所有 skill.md 正确输出到 `.claude/skills/`。

**失败处理**: 检查构建错误日志，定位失败的 skill 文件。

### Step 2: 验证所有插件格式

```bash
node bin/tackle.js validate
```

**预期**: 全部插件验证通过。

**失败处理**: 检查验证失败的具体插件和原因。

### Step 3: 运行全部测试套件

```bash
node --test test/**/*.js
```

**预期**: 全部测试通过。

**失败处理**: 记录失败的测试，分析是否与本次修改相关。

### Step 4: 人工检查修改后的 skill.md 输出

检查 `.claude/skills/` 目录下以下文件的输出内容：

1. `.claude/skills/skill-split-work-package/skill.md` — 确认三位约束已消除
2. `.claude/skills/skill-task-creator/skill.md` — 确认位数无限制注释已添加
3. `.claude/skills/skill-batch-task-creator/skill.md` — 确认混合位数说明已添加
4. `.claude/skills/skill-progress-tracker/skill.md` — 确认四位示例已添加
5. `.claude/skills/skill-agent-dispatcher/skill.md` — 确认四位示例已添加
6. `.claude/skills/skill-completion-report/skill.md` — 确认占位符注释已添加

**检查要点**:
- 不包含 "三位数字" 字样
- 不包含前导零编号示例
- 包含四位编号案例或位数无限制说明

### Step 5: 记录测试结果

汇总所有测试步骤的结果，如全部通过则标记完成。

## 关键文件

- `bin/tackle.js` — CLI 入口
- `.claude/skills/` — 构建输出目录
- `test/**/*.js` — 测试套件

## 验收标准

- [ ] `node bin/tackle.js build` 成功
- [ ] `node bin/tackle.js validate` 全部通过
- [ ] `node --test test/**/*.js` 全部通过
- [ ] 6 个 skill.md 输出文件确认三位约束已消除
- [ ] 所有四位编号示例/注释正确传播到构建输出
