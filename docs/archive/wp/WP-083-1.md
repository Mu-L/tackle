# WP-083-1: 插件发现与安装 - install 命令

## 🤖 Subagent 读取指令

> **重要**: 执行此任务前，请先阅读父工作包文档获取完整上下文：
> - 父工作包: `docs/wp/WP-083.md`
> - 包含: 问题分析、实施计划、关键文件、验收标准

## 基本信息

| 属性 | 值 |
|------|-----|
| **类型** | impl |
| **父工作包** | WP-083 |
| **依赖** | WP-082, WP-085 |
| **预估时间** | 40-45min |
| **执行角色** | implementer |
| **状态** | 📋 待执行 |

## 职责

实现 `tackle install` 命令：从 npm 安装插件并注册到 registry。

## 任务清单

- [ ] 创建 `commands/install.js` 命令模块
- [ ] 实现 npm install 子进程调用
- [ ] 实现安装后 registry 注册 (添加 entry + sourceType: "npm")
- [ ] 实现 manifest 验证 (安装后验证插件格式)
- [ ] 实现 dry-run 模式 (`--dry-run`)
- [ ] 实现网络错误重试机制 (最多 3 次)
- [ ] 实现进度输出
- [ ] 单元测试

## 验收标准

- [ ] `tackle install tackle-plugin-xxx` 成功安装并注册
- [ ] dry-run 模式正确预览操作 (不实际安装)
- [ ] 网络错误有重试机制
- [ ] 安装后 registry 正确更新
- [ ] 安装后 `tackle build` 可识别新插件

## 关键文件

- `commands/install.js` — 新建
- `plugins/plugin-registry.json` — 动态更新
