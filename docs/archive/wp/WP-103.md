# WP-103: 路线图编号体系统一与 Phase 结构调整

## 🤖 Subagent 读取指令

> **重要**: 此文档包含完整的任务上下文。执行前请阅读以下内容：
> - **问题分析**: 路线图编号与 task.md 不一致，Phase 结构需调整
> - **实施计划**: 按 Step 顺序执行
> - **关键文件**: `docs/design/roadmap-v0.2.0.md`
> - **验收标准**: 编号统一、Phase 结构正确

## 基本信息

| 属性 | 值 |
|------|-----|
| **优先级** | P0 |
| **预估AI时间** | 30min |
| **拆分模式** | simple |
| **状态** | ✅ 完成 |

## 目标

将 v0.2.0 路线图 (`docs/design/roadmap-v0.2.0.md`) 的 WP 编号体系统一到 task.md 已使用的编号 (WP-082~101)，并调整 Phase 结构以反映三份审查报告的共识。

## 背景

路线图当前使用混合编号：
- Phase 1: WP-096(CLI模块化), WP-097(路径统一), WP-078(外部插件), WP-079(插件安装), WP-080(Skill测试)
- Phase 2: WP-081~084
- Phase 3: WP-085~093
- Phase 4: WP-094, WP-095

task.md 使用统一编号 WP-082~101。两套编号导致混乱。

## 实施计划

### Step 1: WP 编号映射替换

全文替换旧编号为新编号（注意顺序，避免替换冲突）：

| 旧编号 | 新编号 | 标题 |
|--------|--------|------|
| WP-096 | WP-085 | CLI 模块化重构 |
| WP-097 | WP-086 | Plugin Loader 路径解析统一 |
| WP-078 | WP-082 | 外部插件加载机制 |
| WP-079 | WP-083 | 插件发现与安装 |
| WP-080 | WP-084 | Skill 插件结构性测试 |
| WP-081 | WP-087 | 性能监控与诊断模块 |
| WP-082 | WP-088 | 诊断命令 |
| WP-083 | WP-089 | 插件模板/脚手架 |
| WP-084 | WP-090 | E2E 工作流测试 |
| WP-085 | WP-091 | API 参考文档 |
| WP-086 | WP-092 | 插件版本兼容性检查 |
| WP-087 | WP-093 | TypeScript 类型定义 |
| WP-088 | WP-094 | Changelog 自动生成 |
| WP-089 | WP-095 | 插件发布工具 |
| WP-090 | WP-096 | 性能基准测试 |
| WP-091 | WP-097 | 完整示例项目 |
| WP-092 | WP-098 | 贡献指南更新 |
| WP-093 | WP-099 | 多环境配置支持 |
| WP-094 | WP-100 | 插件权限与安全模型 |
| WP-095 | WP-101 | 远程 Agent 设计研究 |

**替换策略**: 使用临时占位符避免冲突。例如先替换 `WP-096` → `__WP_085__`，最后统一 `__WP_XXX__` → `WP-XXX`。

### Step 2: 新增 Phase 0

在 Section 2 (Phase 1) 之前插入 Phase 0：

```markdown
## 1.5 Phase 0: 前置修复

**目标**: 修复已知问题，为 Phase 1 执行扫清障碍
**预估总时间**: ~5 分钟

### WP-082-pre: package-lock.json 版本修复

| 属性 | 值 |
|------|-----|
| 优先级 | P0 (前置) |
| 复杂度 | 3 分 (simple) |
| 预估时间 | 5min |
| 依赖 | 无 |
| 状态 | 📋 |

**目标**: 修复 package-lock.json 版本不一致 (0.0.11 vs 0.1.2)。

**实施要点**:
- 执行 `npm install` 同步版本
- 验证 164 测试全通过
- 提交更新后的 package-lock.json

**验收标准**:
- package-lock.json 版本与 package.json 一致
- `node --test test/**/*.js` 全量通过
```

### Step 3: Phase 4 重构

将 Phase 4 修改为：
- WP-100(权限模型设计研究) 标注可在 Phase 2 并行执行
- WP-101 缩减为 "EventBus 远程化可行性评估" (15min)
- Phase 4 不再作为独立阶段，WP-100/101 可提前

### Step 4: 重写全局依赖关系图 (Section 6)

```
Phase 0 (前置):
  WP-082-pre (package-lock 修复) [独立]

Phase 1 (前置 + 核心):
  WP-085 (CLI 模块化) ─────────────────────────────┐
  WP-086 (路径统一) ──► WP-082 (外部插件) ──────────┤
                                  ├──► WP-083 (插件安装) ┘
  WP-084 (Skill 测试) [独立]

Phase 2 (开发体验):
  WP-088 (诊断命令)
  WP-089 (模板)
  WP-090 (E2E 测试) ◄── WP-082
  WP-100 (权限设计研究) [独立, 可提前]

Phase 3 (工具链):
  WP-082 ──► WP-092 (版本检查)
  WP-083 ──► WP-095 (发布清单) ──► WP-098 (贡献指南)
  WP-087 (性能监控) ──► WP-096 (性能基准) [可选]
  WP-082 + WP-089 ──► WP-097 (示例项目)
  WP-091, WP-093, WP-094 [独立]
  WP-099 (多环境) [可选/建议移除]

Phase 4 (提前/缩减):
  WP-101 EventBus 远程化评估 (15min)
```

### Step 5: 更新关键路径

```
WP-086 → WP-082 → WP-083 → WP-095 → WP-098
                    ↘ WP-090
                    ↘ WP-092
                    ↘ WP-097 (with WP-089)

关键路径总时长: 20 + 55 + 90 + 20 + 15 = 200min
```

## 关键文件

- `docs/design/roadmap-v0.2.0.md` — 唯一修改文件

## 验收标准

- [x] 路线图所有 WP 编号与 task.md 一致
- [x] Phase 0 已新增
- [x] 全局依赖关系图已重写
- [x] 关键路径已更新
- [x] 无遗留旧编号引用

## 完成记录

- **完成日期**: 2026-05-28
- **执行者**: documenter-t1 (subagent)
- [ ] 无遗留旧编号引用
