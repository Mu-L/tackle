# WP-065: 构建输出内容验证

## 🤖 Subagent 读取指令

> **重要**: 执行此任务前，请先阅读父工作包文档获取完整上下文：
> - 父工作包: WP-063~WP-067 校验系列
> - WP-064 必须先完成（依赖构建输出）
> - 包含: 源文件与构建输出的对比验证

## 基本信息

| 属性 | 值 |
|------|-----|
| **优先级** | P1 |
| **预估AI时间** | 8min |
| **拆分模式** | standard（父工作包的一部分） |
| **状态** | ✅ 完成 |
| **依赖** | WP-064（构建必须完成） |
| **执行角色** | reviewer |

## 目标

验证 WP-057~WP-060 的源文件变更已正确传播到 `.claude/skills/` 下的构建输出文件中。

## 校验范围

### 构建输出文件（6 个）

- `.claude/skills/skill-split-work-package/skill.md`
- `.claude/skills/skill-task-creator/skill.md`
- `.claude/skills/skill-batch-task-creator/skill.md`
- `.claude/skills/skill-progress-tracker/skill.md`
- `.claude/skills/skill-agent-dispatcher/skill.md`
- `.claude/skills/skill-completion-report/skill.md`

### 校验要点

**负面校验（不应存在的内容）**：
- [ ] `grep "三位数字" .claude/skills/*/skill.md` → 0 匹配
- [ ] `grep -E "WP-00[0-9]" .claude/skills/*/skill.md` → 0 匹配（无前导零编号）
- [ ] 无过时/缓存内容

**正面校验（应存在的内容）**：
- [ ] 位数无限制注释/说明存在于对应输出文件
- [ ] 四位数示例存在于 WP-059 和 WP-060 对应的输出文件
- [ ] 跨 999 边界示例存在于 skill-batch-task-creator 输出

**源文件 vs 构建输出对比**：
- [ ] 每个构建输出文件与其源文件内容一致（构建管道无内容丢失）
- [ ] 无额外的构建时注入内容干扰

## 预期产出

- `docs/reports/wp-065-build-output-verification.md` — 构建输出验证报告，包含：
  - 每个输出文件的 grep 结果
  - 源文件 vs 构建输出对比表
  - 传播问题列表（如有）

## 验收标准

- [ ] 所有负面校验通过（0 匹配）
- [ ] 所有正面校验通过（变更已传播）
- [ ] 验证报告已输出到 docs/reports/
