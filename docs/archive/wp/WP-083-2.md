# WP-083-2: 插件发现与安装 - uninstall 命令

## 🤖 Subagent 读取指令

> **重要**: 执行此任务前，请先阅读父工作包文档获取完整上下文：
> - 父工作包: `docs/wp/WP-083.md`
> - 包含: 问题分析、实施计划、关键文件、验收标准

## 基本信息

| 属性 | 值 |
|------|-----|
| **类型** | impl |
| **父工作包** | WP-083 |
| **依赖** | WP-083-1 |
| **预估时间** | 25-30min |
| **执行角色** | implementer |
| **状态** | 📋 待执行 |

## 职责

实现 `tackle uninstall` 命令：卸载插件并清理 registry。

## 任务清单

- [ ] 创建 `commands/uninstall.js` 命令模块
- [ ] 实现依赖检查 (其他插件是否依赖目标插件)
- [ ] 实现确认提示 (需卸载的插件列表)
- [ ] 实现 npm uninstall 子进程调用
- [ ] 实现 registry 清理 (移除 entry)
- [ ] 实现 `.claude/` 输出清理
- [ ] 实现 dry-run 模式
- [ ] 单元测试

## 验收标准

- [ ] `tackle uninstall tackle-plugin-xxx` 成功卸载并清理
- [ ] 依赖检查阻止卸载被依赖的插件 (输出依赖列表)
- [ ] dry-run 模式正确预览操作
- [ ] 卸载后 registry 正确更新
- [ ] 卸载后 `tackle build` 不再包含该插件

## 关键文件

- `commands/uninstall.js` — 新建
- `plugins/plugin-registry.json` — 动态更新
