# WP-083-3: 插件发现与安装 - search 命令

## 🤖 Subagent 读取指令

> **重要**: 执行此任务前，请先阅读父工作包文档获取完整上下文：
> - 父工作包: `docs/wp/WP-083.md`
> - 包含: 问题分析、实施计划、关键文件、验收标准

## 基本信息

| 属性 | 值 |
|------|-----|
| **类型** | impl |
| **父工作包** | WP-083 |
| **依赖** | WP-083-1 |
| **预估时间** | 15-20min |
| **执行角色** | implementer |
| **状态** | 📋 待执行 |

## 职责

实现 `tackle search` 命令：按 `tackle-plugin-*` 前缀搜索 npm registry。

## 任务清单

- [ ] 创建 `commands/search.js` 命令模块
- [ ] 实现 npm registry 搜索 API 调用
- [ ] 按 `tackle-plugin-*` 前缀过滤
- [ ] 结果格式化输出 (名称、版本、描述)
- [ ] registry 源切换支持 (`--registry`)
- [ ] 网络错误处理
- [ ] 单元测试

## 验收标准

- [ ] `tackle search` 输出 `tackle-plugin-*` 前缀的插件列表
- [ ] 结果包含名称、版本、描述
- [ ] 支持 `--registry` 指定自定义 registry
- [ ] 网络错误有友好提示

## 关键文件

- `commands/search.js` — 新建
