# WP-120-2-test: Manifest 扩展测试

## 🤖 Subagent 读取指令

> **重要**: 执行此任务前，请先阅读父工作包文档获取完整上下文：
> - 父工作包: `docs/wp/WP-120.md`
> - 前置工作包: `docs/wp/WP-120-1-impl.md`（Manifest 扩展实现）

## 基本信息

| 属性 | 值 |
|------|-----|
| **类型** | test |
| **父工作包** | WP-120 |
| **依赖** | WP-120-1-impl |
| **执行角色** | tester |
| **状态** | 📋 待执行 |
| **预估时间** | 15min |

## 职责

编写 Manifest 扩展功能的单元测试，覆盖外部插件注册、合并逻辑、重复处理和错误处理。

## 任务清单

### Step 1: 项目级外部插件注册测试

- [ ] 测试从 `harness-config.yaml` 读取 `externalPlugins` 配置
- [ ] 测试 `local` 类型外部插件的路径解析
- [ ] 测试 `npm` 类型外部插件的标准解析
- [ ] 测试 `git` 类型外部插件的解析
- [ ] 测试外部插件 manifest 对象的标准化格式

### Step 2: 合并逻辑测试

- [ ] 测试 core 插件与外部插件的正确合并
- [ ] 测试合并后插件列表包含所有 core 和外部插件
- [ ] 测试合并顺序: core 插件优先
- [ ] 测试空外部插件列表时仅返回 core 插件

### Step 3: 重复插件处理测试

- [ ] 测试外部插件与 core 插件同名时的处理（保留 core 版本）
- [ ] 测试重复插件产生 warning 日志
- [ ] 测试多个外部插件之间的同名处理

### Step 4: 错误处理测试

- [ ] 测试无效 `source` 路径时的错误处理
- [ ] 测试缺少必填字段时的错误提示
- [ ] 测试不支持的 `sourceType` 时的错误处理
- [ ] 测试配置文件格式错误时的优雅降级

## 关键文件

### 输入（读取）
- `plugins/runtime/manifest-resolver.js` — 被测模块
- `docs/wp/WP-120-1-impl.md` — 实现文档（理解接口定义）

### 输出（新建）
- `test/runtime/test-manifest-resolver.js` — 单元测试

## 验收标准

- [ ] 所有测试用例通过
- [ ] 覆盖正常注册、合并、重复处理、错误处理四类场景
- [ ] 测试不依赖外部环境（使用 mock/stub）
