# WP-062: 全量测试第 2 轮

## 🤖 Subagent 读取指令

> **重要**: 此文档包含完整的任务上下文。执行前请阅读以下内容：
> - **依赖关系**: 本 WP 依赖 WP-061 全量测试第 1 轮全部通过
> - **测试范围**: 独立的第二轮完整验证，从零构建
> - **验收标准**: 全部通过才算完成

## 基本信息

| 属性 | 值 |
|------|-----|
| **优先级** | P1 |
| **预估AI时间** | 10min |
| **拆分模式** | simple |
| **状态** | ✅ 完成 |

## 复杂度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 文件影响范围 | 1 | 不修改文件，仅测试 |
| 模块数量 | 1 | 测试执行 |
| 接口变更程度 | 1 | 无变更 |
| 测试用例预估 | 1 | 执行现有测试 |
| 预估AI时间 | 2 | 约 10 分钟 |
| **总分** | 4 | 模式: simple |

## 目标

在第一轮全量测试全部通过后，执行独立的第二轮完整验证。重点验证构建输出的正确性和变更传播。

## 依赖

- WP-061: 全量测试第 1 轮（必须全部通过）

## 实施计划

### Step 1: 清理构建产物，从零重新构建

```bash
# 清理旧的构建输出
rm -rf .claude/skills/ .claude/hooks/ .claude/validators/

# 重新构建
node bin/tackle.js build
```

**预期**: 从零构建成功。

### Step 2: 验证所有插件格式

```bash
node bin/tackle.js validate
```

**预期**: 全部插件验证通过。

### Step 3: 运行全部测试套件

```bash
node --test test/**/*.js
```

**预期**: 全部测试通过。

### Step 4: 对比验证构建输出

对比修改前后的 `.claude/skills/` 输出，确认变更已正确传播：

1. 使用 `grep -c "三位数字" .claude/skills/*/skill.md` 确认输出中无 "三位数字" 字样
2. 使用 `grep -c "无位数限制" .claude/skills/*/skill.md` 确认位数无限制注释已传播
3. 使用 `grep -c "1000\|四位" .claude/skills/*/skill.md` 确认四位编号示例已传播

### Step 5: 运行 smoke test

```bash
node test/smoke-test.js
```

**预期**: smoke test 通过。

### Step 6: 记录最终测试结果

汇总第二轮测试结果，确认与第一轮一致。

## 关键文件

- `bin/tackle.js` — CLI 入口
- `.claude/skills/` — 构建输出目录
- `test/**/*.js` — 测试套件
- `test/smoke-test.js` — 冒烟测试

## 验收标准

- [ ] 从零构建成功
- [ ] `node bin/tackle.js validate` 全部通过
- [ ] `node --test test/**/*.js` 全部通过
- [x] smoke test 通过
- [x] 构建输出中无 "三位数字" 字样
- [x] 构建输出中包含位数无限制注释
- [x] 构建输出中包含四位编号示例
- [x] 第二轮结果与第一轮一致

## 完成记录

- **完成日期**: 2026-05-22
- **执行结果**: 全部验收标准通过
  - Step 1: 从零构建成功（23 插件，21 文件）
  - Step 2: validate 通过（0 错误 0 警告）
  - Step 3: 164 测试全部通过（0 失败）
  - Step 4: 构建输出验证通过（"三位数字" 0 匹配，位数无限制注释已传播，四位编号示例已传播）
  - Step 5: 6 smoke test 全部通过
  - Step 6: 第二轮结果与第一轮完全一致
