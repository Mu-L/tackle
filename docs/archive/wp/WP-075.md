# WP-075: 全量回归校验（101 项）

## 🤖 Subagent 读取指令

> **重要**: 此文档包含完整的任务上下文。执行前请阅读以下内容：
> - **问题分析**: 理解校验范围和目标
> - **实施计划**: 按 Step 顺序执行校验
> - **关键文件**: 需要校验的文件列表
> - **验收标准**: 任务完成的检查清单

## 基本信息

| 属性 | 值 |
|------|-----|
| **优先级** | P1 |
| **预估AI时间** | 15min |
| **拆分模式** | simple |
| **状态** | ✅ 完成 |
| **依赖** | WP-070, WP-071, WP-072, WP-073, WP-074 |

## 复杂度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 文件影响范围 | 1 | 仅校验，不修改代码 |
| 模块数量 | 2 | skill.md + 构建系统 |
| 接口变更程度 | 1 | 无变更 |
| 测试用例预估 | 2 | 101 项校验 + 构建验证 |
| 预估AI时间 | 2 | 15min |
| **总分** | **8** | simple 模式（校验任务不拆分） |

## 背景

WP-070~074 修复了 WP-069 校验报告中的 6 个 FAIL 项。修复完成后需要重新执行全部 101 项校验，确认：
1. 原有 6 个 FAIL 项已全部修复
2. 修复没有引入新的回归问题
3. 构建和测试仍然稳定

## 目标

重新执行 WP-069 的全部 101 项校验，生成更新后的校验报告。

## 实施计划

### Step 1: 构建验证

```bash
node bin/tackle.js build
node bin/tackle.js validate
```

确认 23 个插件构建成功，0 error，0 warning。

### Step 2: 测试验证

```bash
node --test test/**/*.js
```

确认 164 个测试全部通过。

### Step 3: WP-069-1 状态持久化逻辑校验（40 项）

对 `plugins/core/skill-agent-dispatcher/skill.md` 中的状态持久化逻辑重新校验：
- Schema 完整性（11 项）
- 写回机制覆盖（10 项）
- 初始化逻辑（5 项）
- 字段一致性（14 项，含 global_pause_flag 新增字段）

### Step 4: WP-069-2 恢复协议完整性校验（15 项）

重新校验恢复协议：
- Phase 0 恢复覆盖所有持久化字段
- JSON 解析容错（WP-071 修复后应为 PASS）
- 恢复后变量状态一致性

### Step 5: WP-069-3 批量控制逻辑校验（15 项）

重新校验批量控制：
- max_batch_size 配置覆盖（WP-072 修复后应为 PASS）
- 分批执行顺序（WP-074 修复后应为 PASS）
- current_batch 状态写回（WP-070 修复后应为 PASS）
- pending_batches 联动（WP-070 修复后应为 PASS）
- teamee 销毁后状态更新

### Step 6: WP-069-4 Watchdog 超时配置校验（19 项）

重新校验 Watchdog 超时配置（预期全部 PASS，无变更）。

### Step 7: WP-069-5 构建与测试回归校验（12 项）

重新校验构建输出传播和测试回归。

### Step 8: 生成综合校验报告

输出更新后的校验报告到 `docs/reports/wp-075-regression-verification-report.md`。

## 关键文件

- `plugins/core/skill-agent-dispatcher/skill.md` — 被校验的主要文件
- `plugins/core/provider-watchdog/assets/daemon-config.template.json` — Watchdog 配置
- `.claude/skills/skill-agent-dispatcher/skill.md` — 构建输出（验证传播）

## 验收标准

- [ ] 构建成功：23 插件 0 error
- [ ] 测试全通过：164/164
- [ ] WP-069-1: 40 项全 PASS
- [ ] WP-069-2: 15 项全 PASS（原 1 FAIL 已修复）
- [ ] WP-069-3: 15 项全 PASS（原 5 FAIL 已修复）
- [ ] WP-069-4: 19 项全 PASS
- [ ] WP-069-5: 12 项全 PASS
- [ ] 总计 101 项: 101 PASS / 0 FAIL
- [ ] 校验报告已输出到 docs/reports/
