# WP-105: 路线图 WP 范围与依赖关系调整

## 🤖 Subagent 读取指令

> **重要**: 此文档包含完整的任务上下文。执行前请阅读以下内容：
> - **问题分析**: 多个 WP 的范围、优先级、依赖关系需要调整
> - **实施计划**: 按 Step 逐一修改各 WP 描述
> - **关键文件**: `docs/design/roadmap-v0.2.0.md`
> - **验收标准**: 所有 WP 范围调整与用户决策一致

## 基本信息

| 属性 | 值 |
|------|-----|
| **优先级** | P0 |
| **预估AI时间** | 25min |
| **拆分模式** | simple |
| **状态** | ✅ 完成 |

## 目标

根据三份审查报告的共识和用户决策，调整路线图中各 WP 的范围、优先级和依赖关系。

## 用户决策摘要

- WP-087(性能监控): **降级为 P3 可选**
- WP-099(多环境配置): **移除**
- WP-101(远程Agent): **缩减为 EventBus 远程化评估 (15min)**
- 新增项: **全部纳入**

## 实施计划

### Step 1: WP-087 性能监控降级为 P3 可选

将 WP-087 从 Phase 2 移到 Phase 3 末尾，标注为 P3 可选。

**修改内容**:
- 优先级: P1 → **P3 (可选)**
- 在 WP-087 标题后加 "(可选)"
- 增加说明: "CLI 非常驻进程，性能数据 ROI 极低。仅在多个用户明确有需求时实施。"
- 如实施，保持事件驱动方案不变

### Step 2: WP-099 多环境配置移除

从 Phase 3 移除 WP-099 整个章节。

**理由**: ConfigManager 已支持环境变量 + 三层配置，无实际场景。三份报告一致建议移除。

**修改内容**:
- 删除 WP-099 章节全部内容
- Phase 3 依赖关系图删除 WP-099
- Phase 3 WP 数和时间汇总更新

### Step 3: WP-095 发布工具缩减范围

将 WP-095 从"插件发布工具"缩减为"发布检查清单"。

**修改内容**:
- 标题: "插件发布工具" → "发布检查清单"
- 目标: 从"提供插件发布工具链"改为"提供插件发布前的检查清单"
- 实施要点:
  - 移除 `tackle publish` 命令计划
  - 保留: 发布检查清单 (plugin.json 格式、npm 发布配置、README 存在)
  - 可选: GitHub Actions 模板
- 时间: 25min → **15-20min**
- 复杂度: 11 分 → **7 分**

### Step 4: WP-088 诊断命令移除对 WP-087 依赖

**修改内容**:
- 依赖: WP-081, WP-096 → **WP-085** (仅 CLI 模块化)
- 实施要点增加: 独立性能检查项（直接采集插件加载时间，不依赖 WP-087 模块）
- 验收标准更新: 性能数据通过独立方式获取

### Step 5: WP-083 插件安装增加拆分建议

在 WP-083 中增加拆分建议说明。

**修改内容**:
- 增加"拆分建议"子节:
  ```
  建议 WP-083 拆分为 3 个子 WP:
  - WP-083-1: install 命令 (40-45min)
  - WP-083-2: uninstall 命令 (25-30min)
  - WP-083-3: search 命令 (15-20min)

  三者可独立实施，install 优先级最高。
  ```
- 保持 WP-083 主体描述不变（拆分为建议而非强制）

### Step 6: 新增缺失项

#### 6a. Phase 0: package-lock.json 版本修复

在 Phase 0 新增一个前置 WP:
- 标题: package-lock.json 版本修复
- 时间: 5min
- 内容: `npm install` 同步版本 + 验证 164 测试

#### 6b. Phase 3: harness-build.js 模块化评估

在 Phase 3 末尾新增:
- 标题: harness-build.js 模块化评估
- 时间: 15min
- 内容: 评估 1,548 行单文件的拆分可行性，产出评估报告
- 依赖: WP-085 (参考 CLI 模块化经验)

#### 6c. WP-082 验收标准增加

在 WP-082(外部插件加载) 的验收标准中增加:
- "产出 `docs/plugin-package-convention.md`，定义 `tackle-plugin-*` 包的目录结构约定"

### Step 7: WP-101 缩减为 EventBus 远程化评估

**修改内容**:
- 标题: "远程 Agent 设计研究" → "EventBus 远程化可行性评估"
- 目标: 从"产出远程 Agent 架构设计文档"改为"评估 EventBus pub/sub 模式远程化可行性"
- 研究要点:
  - EventBus pub/sub → WebSocket/IPC 传输可行性
  - Agent 注册、发现、通信协议方向性评估
  - 产出: 1 页方向性文档（如不可行记录否定结论）
- 时间: 20min → **15min**
- 复杂度: 8 分 → **5 分**

### Step 8: 更新各 Phase 依赖关系图

**Phase 1** (不变):
```
WP-085 (CLI 模块化) ──────────────────┐
                                       ├──► WP-083 (插件安装)
WP-086 (路径统一) ──► WP-082 (外部插件) ┘
WP-084 (Skill 测试) [独立]
```

**Phase 2** (WP-088 移除 WP-087 依赖):
```
WP-088 (诊断命令) [独立, 移除对 WP-087 依赖]
WP-089 (模板) ◄── WP-085
WP-090 (E2E 测试) ◄── WP-082
WP-100 (权限设计研究) [独立, 从 Phase 4 提前]
WP-101 (EventBus 远程化评估) [独立, 从 Phase 4 提前]
```

**Phase 3** (移除 WP-099):
```
WP-082 ──► WP-092 (版本检查)
WP-083 ──► WP-095 (发布清单) ──► WP-098 (贡献指南)
WP-087 (性能监控) ──► WP-096 (性能基准) [可选]
WP-082 + WP-089 ──► WP-097 (示例项目)
WP-091, WP-093, WP-094 [独立]
harness-build.js 评估 ◄── WP-085
```

### Step 9: 更新 Phase 里程碑

**Phase 0**:
- **M0**: package-lock.json 修复完成 → CI 全绿

**Phase 2** (更新):
- **M5**: WP-088 完成 → `tackle doctor` 可用
- **M6**: WP-089 完成 → `tackle generate` 可用
- **M7**: WP-090 完成 → E2E 工作流有保障
- **M8**: WP-100/101 完成 → 企业级设计文档

## 关键文件

- `docs/design/roadmap-v0.2.0.md` — 唯一修改文件

## 验收标准

- [ ] WP-087 降级为 P3 可选
- [ ] WP-099 已移除
- [ ] WP-095 缩减为发布检查清单
- [ ] WP-088 不依赖 WP-087
- [ ] WP-083 包含拆分建议
- [ ] Phase 0 已新增
- [ ] harness-build.js 评估 WP 已新增
- [ ] WP-082 验收标准包含包结构约定文档
- [ ] WP-101 缩减为 EventBus 远程化评估
- [ ] 依赖关系图已更新
