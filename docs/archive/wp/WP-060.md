# WP-060: 修复 3 个 Skill 文档的低位数示例暗示 [Low]

## 🤖 Subagent 读取指令

> **重要**: 此文档包含完整的任务上下文。执行前请阅读以下内容：
> - **问题分析**: 理解任务的背景和问题点
> - **实施计划**: 按 Step 顺序执行
> - **关键文件**: 需要修改的文件列表
> - **验收标准**: 任务完成的检查清单

## 基本信息

| 属性 | 值 |
|------|-----|
| **优先级** | P2 |
| **预估AI时间** | 5min |
| **拆分模式** | simple |
| **状态** | ✅ 完成 |

## 复杂度评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 文件影响范围 | 2 | 3 个文件但均为小改动 |
| 模块数量 | 1 | 仅文档修改 |
| 接口变更程度 | 1 | 无逻辑变更 |
| 测试用例预估 | 1 | 无需新增测试 |
| 预估AI时间 | 1 | 约 5 分钟 |
| **总分** | 5 | 模式: simple |

## 目标

消除 `skill-progress-tracker`、`skill-agent-dispatcher`、`skill-completion-report` 中的三位编号示例暗示，添加四位编号案例以预防 AI 编号固定位数假设。

## 问题分析

### 来源

- WP-055 兼容性分析报告第 3.5-3.7 节（Low 风险）
- WP-056 验证报告（确认所有行号和内容准确）

### 三个文件的问题

1. **skill-progress-tracker**: 行 89, 104, 134, 169 使用 `WP-109`、`WP-029~035` 三位示例
2. **skill-agent-dispatcher**: 行 46-56, 163-164, 178, 753 使用 `WP-037~039`、`WP073` 三位示例
3. **skill-completion-report**: 行 67, 83, 97, 114-120, 161-178 使用 `WP-XXX` 占位符（虽无位数限制但需明确说明）

## 实施计划

### Step 1: 修改 skill-progress-tracker（行 89, 104, 134）

在关键示例区域添加四位编号案例：

1. 行 89 附近（工作包完成示例表）：
   - 在 `WP-109` 旁添加 `WP-1009` 的示例行

2. 行 104 附近（最近完成模板表）：
   - 在示例中添加一个四位编号行

3. 行 134 附近（状态更新示例）：
   - 在 `WP-029~035` 旁添加 `WP-1029~1035` 说明

### Step 2: 修改 skill-agent-dispatcher（行 46-56, 163-164, 753）

1. 行 163-164 附近（团队命名规范）：
   - 添加四位编号示例：`batch-20260514-WP1037`
   - 添加批量四位示例：`batch-20260514-WP1037-1040`

2. 行 753 附近（循环依赖检测示例）：
   - 在 `WP-037 → WP-038 → WP-039 → WP-037` 下方添加：
   `（四位编号示例: WP-1037 → WP-1038 → WP-1039 → WP-1037）`

### Step 3: 修改 skill-completion-report（行 67, 97, 114-120）

1. 行 67 附近（文档同步表）：
   - 在 `docs/wp/WP-XXX.md` 旁添加注释：`（XXX 为数字编号，支持任意位数）`

2. 行 97 附近（门控验证命令）：
   - 在 `WP-XXX` 旁添加注释：`（替换为实际编号，如 WP-56 或 WP-1056）`

3. 行 114-120 附近（报告模板）：
   - 在 `WP-XXX: 工作包名称` 处添加注释：`<!-- XXX = 实际数字编号，无位数限制 -->`

### Step 4: 验证修改

逐一读取三个修改后的文件，确认四位编号示例已正确添加，不影响原有内容。

## 关键文件

- `plugins/core/skill-progress-tracker/skill.md`
- `plugins/core/skill-agent-dispatcher/skill.md`
- `plugins/core/skill-completion-report/skill.md`

## 验收标准

- [ ] skill-progress-tracker 包含四位编号示例
- [ ] skill-agent-dispatcher 包含四位编号示例
- [ ] skill-completion-report 占位符处有位数无限制注释
- [ ] 三个文件的修改均未破坏原有内容

## 完成记录

- **完成日期**: 2026-05-22
- **实际工时**: 5min
